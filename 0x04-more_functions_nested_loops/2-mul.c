#include "main.h"

/**
 * mul - multiplies two integers
 * @p: The first number to multiply
 * @q: The second number to multiply with first number
 *
 * Return: The result of multiplication between p parameter and q parameters
 */
int mul(int p, int q)
{
	return (p * q);
}
